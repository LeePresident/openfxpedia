# Agent Context Update

Add entries to the agent context for the following runtime and libs:

- Flutter SDK
- Hive for local storage
- HTTP exchange API client
- Workmanager / background scheduler (Android)

This document is a placeholder describing the required context keys and values.
