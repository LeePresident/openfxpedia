# Observability

- Add structured logging via a simple logger wrapper.
- Expose basic metric counters for conversions and cache hits.
- Ship logs to local file for Windows; consider integration with a remote sink.
