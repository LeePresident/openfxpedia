# OpenFXpedia v1.0.1 — Maintenance release

## One-line summary
A maintenance release that completes app-wide internationalization for the currency encyclopedia and related UI.

---

## Highlights
- App-wide i18n for the currency encyclopedia and surrounding UI.
- Version bump to `1.0.1` with release metadata updated to match.
- Changelog entries updated for in-app release notes and GitHub release publishing.

## What’s new
- Localization:
  - Completed internationalization across the currency encyclopedia and related screens.
  - Updated release metadata so the app surfaces the new version consistently.
- Versioning:
  - Bumped the package version to `1.0.1`.
  - Updated Windows version resources and output filename metadata to match `1.0.1`.
- Release data:
  - Updated `CHANGELOG.md`.
  - Updated `assets/data/changelog_entries.json` so the in-app changelog includes v1.0.1.

## Bug fixes & polish
- Resolved the remaining untranslated UI surface in the encyclopedia flow.
- Synchronized release-facing metadata with the new version number.

## How to install / assets
Attach platform artifacts to this release (recommended filenames):
- `openfxpedia_1.0.1.apk`
- `openfxpedia_1.0.1.apk.sha1`
- `openfxpedia_1.0.1.exe`
- `openfxpedia_1.0.1_setup.exe`

The portable EXE is written to `build/windows/portable/` and the NSIS installer is written to `build/windows/installer/`.
If `makensis` is missing, install NSIS and ensure it is on PATH before running the Windows build script.

Users can check for updates in-app via Settings → App version → Check updates, then download the matching release asset for their device (`openfxpedia_<version>_setup.exe` on Windows or `openfxpedia_<version>.apk` on Android).

## Build commands (for maintainers)
Run the prepared scripts to build and produce versioned artifacts.

Bash (POSIX):

```bash
./scripts/build_android.sh
./scripts/build_windows.sh
```

PowerShell (Windows):

```powershell
./scripts/build_android.ps1
./scripts/build_windows.ps1
```

## Tagging & release notes
- Recommended tag format: `v1.0.1`.
- Paste this changelog into the GitHub release body; the app fetches full release notes from GitHub releases.

## Maintainer checklist
- [ ] Attach the NSIS installer for Windows distribution.
- [ ] Attach `openfxpedia_1.0.1.exe` and `openfxpedia_1.0.1_setup.exe` artifacts.
- [ ] Confirm the in-app changelog data is up to date before publishing.
- [ ] Optionally include screenshots demonstrating the localized encyclopedia UI.
